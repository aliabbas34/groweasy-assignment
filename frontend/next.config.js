/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // @groweasy/shared ships compiled JS from its dist/, so no transpile needed.
};

module.exports = nextConfig;
